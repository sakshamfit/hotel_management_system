node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Migration output: manual worklist of occupied rooms with no guest data
# (contains production guest/room info — never commit).
needs-review.json
